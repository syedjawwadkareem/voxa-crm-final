# ── Public URLs (change <YOUR_VPS_IP> to your actual VPS IP or domain) ──────
# These are baked into the Next.js client bundle at BUILD time via docker-compose build args.
# If you change these you must rebuild the image: docker compose up -d --build
NEXT_PUBLIC_API_URL=http://<YOUR_VPS_IP>/api/v1
